# Better NXU v1.0.2 测试指南

本次修复了多个严重 Bug，请按照以下步骤验证修复效果。

---

## 🔥 关键修复 - 验证码识别卡死问题

### 问题描述
- 用户反馈：登录教务系统时，页面显示"下载识别模型可能需要花费一些时间，请耐心等待..."后一直卡住
- 根本原因：`AddTesseract()` 将 `unsafeWindow.Tesseract` 错误覆盖为 `undefined`，导致验证码识别函数完全失效

### 测试步骤
1. 在 ScriptCat 中安装修复后的脚本（v1.0.2）
2. 访问教务系统登录页（直接访问或通过 WebVPN）
3. 如果已配置账号密码并开启自动登录，观察验证码识别过程
4. **预期结果**：
   - 看到 Toast 提示"正在识别验证码，首次使用需下载识别模型（约 4MB），请耐心等待..."
   - 控制台显示识别进度："核心加载" → "初始化" → "加载语言训练数据" → "初始化接口" → "识别验证码"
   - 识别完成后 Toast 自动消失，页面自动提交登录
   - 如果识别失败，显示错误提示："验证码识别失败: xxx，请手动输入"

### 验证点
- [x] 验证码识别不再卡死
- [x] 能看到详细的识别进度日志
- [x] 识别失败时有明确的错误提示
- [x] 识别成功后能自动登录

---

## 🔧 其他关键修复

### 1. 微信登录页面空指针异常

**位置**：`open.weixin.qq.com`  
**问题**：`weixinLogin()` 中 `!A && !A.innerHTML` 写法导致 A 为 null 时崩溃  
**测试**：访问微信扫码登录页，检查控制台是否有报错

### 2. 授权失败不跳转

**位置**：`ids.nxu.edu.cn/authserver/callback`  
**问题**：`idsReLogin()` 条件判断写反，授权失败时应该跳转但不跳转  
**测试**：触发多因子认证授权失败场景（需要实际环境）

### 3. 教务系统自定义菜单不显示

**位置**：教务系统主页左侧菜单  
**问题**：`menu_dd_all_grade.class` 应为 `className`  
**测试**：
1. 在设置中勾选"全部学期成绩"自定义菜单
2. 访问教务系统主页
3. 检查左侧菜单是否正确添加了"全部学期成绩"条目

### 4. 页面无限卡死问题

**修复的函数**：
- `weixinLogin()` - 微信快速登录按钮等待
- `webvpnLogin()` - 表单填充等待
- `jwglMain()` - 菜单加载等待
- `jwglCourseIframe()` - 课表 iframe 加载等待

**测试方法**：
- 在网络较差或页面元素加载失败的情况下，脚本应该在 20-60 秒后超时并显示错误提示
- 控制台会输出类似 "教务系统菜单加载超时" 的日志

---

## 📝 测试清单

### 最小测试（必测）
- [ ] 教务系统登录页验证码识别（核心问题）
- [ ] 检查控制台是否有 JavaScript 报错
- [ ] 自动登录功能是否正常工作

### 完整测试（可选）
- [ ] WebVPN 主页自定义卡片显示
- [ ] 教务系统自定义菜单添加
- [ ] 课表美化功能
- [ ] 微信快速登录（需实际环境）
- [ ] 知网/万方复制功能
- [ ] 设置页面各项配置

---

## 🐛 如何报告问题

如果在测试中发现问题，请提供以下信息：

1. **浏览器和脚本管理器版本**
   - 浏览器：Chrome/Firefox/Edge + 版本号
   - 脚本管理器：Tampermonkey/ScriptCat + 版本号

2. **重现步骤**
   - 具体操作步骤
   - 访问的 URL
   - 开启的配置选项

3. **控制台日志**
   - 打开浏览器开发者工具（F12）
   - 切换到 Console 标签
   - 截图或复制所有 "Better NXU" 相关日志

4. **预期结果 vs 实际结果**
   - 你期望发生什么
   - 实际发生了什么

---

## 📊 已知限制

1. **Tesseract.js 识别准确率**：验证码识别准确率约 60-80%，如果连续失败请手动输入
2. **首次识别需要下载模型**：约 4MB，在国内网络环境下可能需要 10-30 秒
3. **WebVPN 环境限制**：部分功能在 WebVPN 代理环境下可能有兼容性问题

---

## ✅ 验证通过标准

- ✅ 验证码识别不再永久卡死
- ✅ 所有 while 循环都有超时保护
- ✅ 错误时有明确的提示信息
- ✅ 控制台没有 JavaScript 报错（除了预期的业务错误）

---

感谢测试！如果一切正常，我们将提交 PR 到官方仓库。
