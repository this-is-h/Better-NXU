# 更新日志

该格式基于 [Keep a Changelog](https://keepachangelog.com/)

该项目遵循 [语义化版本控制](https://semver.org/)

本项目的更新日志记录如下：

## 1.2.0 - 2026-07-21

学生体验增强：WebVPN 协议工具、智能路由、按需加载 UI、一键填账号。

### Added
- `parseVpnContext()` / `toWebvpnUrl()`：解析 WebVPN 路径中的真实主机，生成代理链接（基于网瑞达 host token 表）
- 控制台全局 `BetterNXU` 对象：版本、URL 工具、waitFor、fillInput（方便调试与扩展）
- 统一认证登录页（未开自动登录时）右下角 **「Better NXU · 填入账号」** 按钮：一键填入已存账号并预填 AES 密文，滑块仍需手动
- 教务备用入口 token 纳入路由表，与 jwgl 域名统一处理

### Changed
- 路由改为按 **真实业务 host + path** 匹配，不再依赖 `Url.indexOf(超长 token)` 散落判断
- `Basic({ vant: true })` 按需加载 Vant：登录页只加载 Toast，门户/设置/工具/课表才加载 Vue UI
- 门户自定义卡片链接改为 `toWebvpnUrl('https://jwgl...')` 生成，主机变更时只改 token 表
- `ConfigVersion` → `7`
- 版本号 **1.2.0**

### Security / 边界
- 统一认证 **不自动过滑块**（有意为之）
- 仅使用用户本人在脚本设置中保存的账号，不做未授权访问

### Tips
1. 重装/刷新脚本后确认版本 **1.2.0**
2. 控制台应看到：`开始运行 v1.2.0` 与 `路由上下文: viaVpn=... realHost=...`
3. 未开自动登录时，登录页右下角可点「填入账号」
4. 开启自动登录时：自动填表 + startLogin；有滑块请手动完成
5. 调试：`BetterNXU.toWebvpnUrl('https://jwgl.nxu.edu.cn/cas.action')`

## 1.1.0 - 2026-07-21

适配统一身份认证（金智 authserver）当前协议：AES 密码加密 + 滑块验证；并修复主页卡片 id 冲突。

### Changed
- **统一认证自动登录**重写：填充账号/密码后优先调用页面 `startLogin()`，由页面 `checkForm` 完成 `encryptPassword` 与提交流程
- 预填 `#saltPassword`（AES-CBC，salt 来自 `#pwdEncryptSalt`），兼容页面已有 `encryptPassword` / CryptoJS
- 统一认证登录页 **不再加载 Tesseract**（线上 `captchaSwitch=2` 为滑块，OCR 无效）
- 出现滑块时 Toast 提示用户手动完成，**不自动过滑块**
- `ConfigVersion` → `6`（会触发一次版本更新配置弹窗）
- `rememberMe` 改为设置 `checked` + `change` 事件，不再只写 `.value`

### Fixed
- 主页「H - 小工具」与「自定义」共用 `data-id=custom` 导致卡片插错区块 → 拆为 `h-tools` / `custom-cards`
- `weixinLogin` 改用 `waitFor`，避免重复 query 与空指针
- 门户主页等待「教务管理平台」卡片增加超时，避免改版后死等

### Tips（测试建议）
1. 脚本猫/油猴重新安装或刷新 `Better NXU.user.js`，确认版本为 **1.1.0**
2. 设置里打开 **WebVPN 自动登录**，填好账号密码
3. 打开 `https://webvpn.nxu.edu.cn/` 或 `https://ids.nxu.edu.cn/authserver/login`
4. 预期：自动填表并点登录；若弹出滑块，**手动拖完**即可进入
5. 控制台应出现 `Better NXU` 日志：`调用页面 startLogin()` 或 `已预填 saltPassword`
6. 主页同时开启「小工具」与「自定义卡片」时，两块应分别显示、互不覆盖
7. 教务系统自动登录（图形码 OCR）行为与 1.0.2 相同，未改逻辑

## 1.0.2 - 2026-07-02

重要的 Bug 修复版本，解决了验证码识别卡死和多处无限循环导致页面卡死的问题。

### Fixed
- **[严重]** 修复了 `AddTesseract()` 中将 `unsafeWindow.Tesseract` 错误覆盖为 `undefined` 导致验证码识别完全失效的 Bug
- **[严重]** 修复了 `GetVerificationCode()` 中未使用 `unsafeWindow.Tesseract` 且缺少错误处理导致 Promise 永远挂起的问题
- **[严重]** 修复了 `weixinLogin()` 中逻辑判断错误 (`&&` 应为 `||`) 导致空指针异常的 Bug
- **[严重]** 修复了 `idsReLogin()` 中条件判断完全写反导致授权失败时不跳转的逻辑错误
- **[严重]** 修复了 `jwglMain()` 中 `menu_dd_all_grade.class` 应为 `className` 的属性名错误
- **[中等]** 修复了 `webvpnLogin()`、`weixinLogin()`、`jwglMain()`、`jwglCourseIframe()` 中多处 while 循环缺少超时保护导致页面永久卡死的问题
- **[中等]** 为 `GetVerificationCode()` 添加了国内可访问的 traineddata CDN (`bootcdn.net`)，解决默认 CDN 被墙导致模型下载失败的问题
- **[中等]** 修复了教务系统验证码使用相对路径在 WebVPN 环境下可能解析错误的问题
- **[改进]** 为 `jwglLogin()` 添加了完整的错误处理和 Toast 移除逻辑，避免"请耐心等待"提示永不消失

### Changed
- 所有可能无限等待的 while 循环现在都有超时保护（一般为 20-60 秒）
- 验证码识别失败时会显示具体错误信息并记录到控制台
- 优化了验证码识别的提示文案，更准确地描述当前状态

### Tips
- 如果验证码识别仍然失败，请检查浏览器控制台是否有网络错误
- 首次使用验证码识别功能需要下载约 4MB 的识别模型，请耐心等待
- 如遇到页面卡死，请刷新页面并查看控制台日志以排查问题

## 1.0.1 - 2025-12-24

非常抱歉，由于我们的一些疏忽，设置页面与教务系统自动登录出现了一些问题，目前已经修复，我们会在以后的发布之前做好测试工作。

### Fixed
- 修复了对于webvpn中教务系统的判定
- 修复了设置中因变量名错误导致的不可用问题


## 1.0.0 - 2025-12-16

🥳🥳🥳1.0版本正式发布~
从22年底萌生了这个想法后，历时三年，经历了学校系统的多次更新，也经历了无数个改代码的夜晚，终于把脚本完善成了想要的样子
在此，我要向所有使用 Better NXU 的用户表示感谢，也向所有为 Better NXU 做出贡献的个人、组织、项目或其它表示敬意
你的每一次使用都是我们重要的一步，你的每一次帮助都是我们前进的动力
当然脚本还不算完美，还有许多功能等待完善，也有许多新功能等待开发，面对系统更新也需要进行更多的适配
一个人的努力是渺小的，也希望能有更多的人参与到 Better NXU 的建设中来，共同提升 NXU 网站的使用体验
希望 Better NXU 越来越好，也希望大家越来越好
🏃无限进步🏃

### Added
- 新增了多因子认证的快速登录模式
- 新增了关于我们页面的致谢名单

### Changed
- 优化了登录页面的判定

### Tips
- 多因子认证的快速登录模式需要在PC端登录微信客户端的情况下才能使用，需要手动进行一次授权（每一次都要），期间会进入一个错误页面，是正常现象，会自动跳转，这是目前最便捷的登录方式了
- 脚本各项功能的默认开启原则是不影响原有体验，因此第一次使用时，务必要进入配置页面开启自己所需的功能
- 知网的自由复制功能开启后即生效，对于想要复制的文字，只需要选中即可复制，无需任何其它操作
- NXU Charge（充电桩状态查看网站）由于学校禁止使用电动车，已经停止更新维护，虽然保留入口，但数据已失去实际作用，且大概率报错
- 谨以此项目祝大家期末顺利，也提前祝大家元旦快乐🎉🎉🎉~


## 0.5.1 - 2025-12-15

### Added
- 新增了关于我们页面


## 0.5.0 - 2025-12-11

我们自行构建了更为清晰好用的配置页面，同时调整了一些功能、卡片的布局。

### Added
- 新增了 `WebVPN` 首页的 “H - 小工具” 栏
    - 站内小工具（从“自定义”栏移动至此）
    - 宁夏大学猫狗图鉴
    - NXU Charge（从“站内小工具”移动至此）
- 新增了设置页面

### Changed
- 优化了部署提示的显示
- 优化了部分功能的显示

### Fixed
- 修复了对于登录页面的判定
- 修复了对于单双周的判定


## 0.4.2 - 2025-09-09

受 `ScriptCat` 平台与学校系统共同升级影响，脚本出现无法匹配网址的问题，对此开展了紧急修复工作。

### Added
- 新增了版本信息与点击反馈

### Changed
- 适配了新的登录界面
- 优化了 README.md 中的图片路径

### Fixed
- 修复了网址无法匹配的问题


## 0.4.1 - 2025-09-05

### Added
- README.md 关于我们

### Changed
- 修改课表美化的样式
    - 字段顺序改为“课程、教师、教室、周次”，以适配教师、教室、周次发生变化的情况
    - 高亮教室字段，以提升阅读体验
    - 对教师为空的情况进行了适配

### Fixed
- 修复了一些逻辑错误的问题
    - 空课表生成时对尔雅课程的判定
    - 课表美化时对单双周的判定
    - 增加了对部分情况下教师与课程名称调换情况的适配


## 0.4.0 - 2025-09-04

针对课表美化、导出与空课表生成等功能在测试中出现的问题，对课表相关功能进行了集中修复与优化。

本次更新修改了导出课表的json格式，此前的旧版本格式作废，空课表生成将无法识别并报警告信息，所有使用旧版本格式的程序均需适配新的格式文件。

### Added
- 新增了基于 `Keep a Changelog` 格式的更新日志

### Changed
- 修改了导出课表的json格式（旧版本json文件无法继续使用）
    - 新增了 `meta.format` `meta.version` `meta.revision` `meta.class_times_detail` 字段，以增强文件可识别性。
    - 新增了 `schedule.day.number.course.id` 字段，以实现对同一课程持续课时的判定。
- 修改了空课表导出的部分逻辑与显示
    - 适配新版本json文件，旧版本无法解析并会弹出警告提示
    - 显示“上/下半节”，表明该时段的第1/2节课为空课，另一节以上/下一时段为准（部分存在此特殊情况）
    - 显示“尔雅”，表明为尔雅课程
- 自动登录 `WebVPN` 时会勾选“7天免登录”
- 优化部分提示词与注释

### Fixed
- 修复了 tesseract 可能不生效的问题
- 修复了课表美化与导出的部分逻辑与显示问题
    - 修复部分逻辑不全所导致的格式错乱问题
    - 适配部分情况下的教师与课程名称调换情况（复现情况：班级课表存在多名教师而个人课表仅一名教师）


## 0.3.5 - 2025-09-01

### Added
- 新建并开放了 Github 仓库 
    - Github 仓库
    - Github 跳转入口
- 新增了一些注释

### Changed
- 删除了部分冗余代码
- 调整了一些说明信息


## 0.3.3 - 2025-08-27

### Added
- 为小工具新增了一些功能
    - 空课表生成


## 0.3.2 - 2025-08-26

### Added
- 新增了课表导出功能（仅支持json格式，校园网访问教务系统还可支持图片导出）
- 新增了脚本设置入口（仅入口，页面编写中）

### Changed
- 调整优化了小工具等页面部署的提示信息
- 调整了自动登录时的提示信息
- 优化了课表美化的一些逻辑

### Fixed
- 修复了小工具的一些问题
    - 教师工号查询页面底部遮挡问题


## 0.3.1 - 2025-07-14

针对 WebVPN 登录页面的变化进行了适配

### Added
- 一个小彩蛋🥚
- 万方数据html阅读页面的自由复制功能

### Changed
- 适配了新的 WebVPN 登录页面
- 调整了知网、万方阅读页面自由复制的提示

### Fixed
- 修复了小工具的一些问题
    - 小工具页面的逻辑判断问题
    - 校园充电桩状态的显示问题


## 0.3.0 - 2025-03-31

### Added
- 为 `WebVPN` 首页新增了一些卡片
    - 学工系统，方便校外填写报备
    - 小工具，里面设有一些方便实用的校内小工具
    - 浙江大学大先生，点击即可跳转大先生对话页面，使用CARSI联盟登录即可使用
- 为小工具新增了一些功能
    - 教师工号查询
    - 校内充电桩状态查看


## 0.2.5 - 2025-01-19

针对系统查询成绩需要加载很久的问题，采取了临时解决方案

### Added
- 为 `WebVPN` 首页新增了一些卡片
    - 信息门户
- 新增了教务管理系统侧边栏全部学期成绩的选项

### Changed
- 优化了部分网址匹配逻辑
- 优化了自动登录的一些流程

### Fixed
- 修复了弹窗无法正常显示的问题
- 修复了默认数据错误导致异常中止的问题


## 0.2.3 - 2025-01-07

### Added
- 为 `WebVPN` 首页新增了一些卡片
    - 学工系统

### Fixed
- 修复了因脚本干扰导致部分 `WebVPN` 网站无法打开的问题


## 0.2.2 - 2025-01-05

### Fixed
- 修复了课表优化的一些问题


## 0.1.1 - 2023-12-20

### Added
- 安装扩展跳转

### Changed
- 突出显示注意信息


## 0.1.0 - 2023-12-20

首次发布在 ScriptCat 平台，具体实现内容与之前的版本信息并未记录
